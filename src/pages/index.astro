---
import Layout from '~/layouts/PageLayout.astro';
---

<Layout
  metadata={{
    title: 'TapRebook — Standardize every interaction on WhatsApp',
    description:
      'Bookings, reminders, coordination, and status updates—on WhatsApp. Always on. Measurable. Brand-consistent.',
    ignoreTitleTemplate: true,
  }}
>
  <!-- HERO (centered; distinct from other pages) -->
  <section class="relative">
    <div class="max-w-5xl mx-auto px-4 pt-16 md:pt-24 text-center">
      <span class="inline-flex items-center gap-2 rounded-full border border-white/50 bg-white/40 backdrop-blur px-3 py-1 text-xs md:text-sm font-semibold text-slate-800/90 shadow-sm">
        WhatsApp-first, operations-ready
      </span>

      <h1 class="mt-4 text-4xl md:text-6xl font-extrabold tracking-tight">
        Standardize every interaction on WhatsApp
      </h1>

      <p class="mt-4 text-lg md:text-xl text-slate-700/90 max-w-3xl mx-auto">
        Booking, reminders, coordination, and updates—<strong>built to improve operational efficiency</strong> and keep teams in sync.
      </p>

      <p class="mt-3 text-xs md:text-sm text-slate-600/90">
        <span class="rounded-full px-2.5 py-0.5 ring-1 ring-white/30 bg-white/30 backdrop-blur">Always on</span>
        Responds instantly—day or night.
      </p>

      <div class="mt-8 flex items-center justify-center gap-3">
        <a class="btn btn-primary" href="/modules/">Explore solutions</a>
        <a class="btn btn-secondary" href="/contact/">Book a demo</a>
      </div>
    </div>

    <!-- Full-width hero banner image (unique vs. other pages) -->
    <div class="max-w-6xl mx-auto px-4 mt-10">
      <div class="relative rounded-2xl overflow-hidden bg-white/55 ring-1 ring-white/40 shadow-lg backdrop-blur-sm">
        <img
          src="/images/AI_homepage.png"
          alt="AI handling bookings, reminders, and coordination on WhatsApp"
          class="block w-full h-full object-cover"
          width="2000"
          height="1100"
          loading="eager"
          decoding="async"
          sizes="(min-width: 1024px) 960px, 92vw"
        />
        <div class="pointer-events-none absolute inset-0 bg-gradient-to-tr from-white/40 via-transparent to-transparent"></div>
      </div>
    </div>
  </section>

  <!-- THIN STAT BAR (distinct, compact) -->
  <section class="py-6">
    <div class="max-w-6xl mx-auto px-4 grid gap-3 sm:grid-cols-2 lg:grid-cols-4">
      <div class="rounded-xl p-4 ring-1 ring-white/30 bg-white/40 backdrop-blur text-center shadow-sm">
        <div class="text-2xl font-extrabold tracking-tight">↓ 20–40%</div>
        <div class="text-xs text-slate-600">No-shows</div>
      </div>
      <div class="rounded-xl p-4 ring-1 ring-white/30 bg-white/40 backdrop-blur text-center shadow-sm">
        <div class="text-2xl font-extrabold tracking-tight">↑ 2–3×</div>
        <div class="text-xs text-slate-600">Self-serve changes</div>
      </div>
      <div class="rounded-xl p-4 ring-1 ring-white/30 bg-white/40 backdrop-blur text-center shadow-sm">
        <div class="text-2xl font-extrabold tracking-tight">↓ 30–50%</div>
        <div class="text-xs text-slate-600">Wait-time calls</div>
      </div>
      <div class="rounded-xl p-4 ring-1 ring-white/30 bg-white/40 backdrop-blur text-center shadow-sm">
        <div class="text-2xl font-extrabold tracking-tight">&lt; 5s</div>
        <div class="text-xs text-slate-600">Median first response</div>
      </div>
    </div>
  </section>

  <!-- HOW IT WORKS (horizontal stepper; new look) -->
  <section class="py-10 border-t border-white/20">
    <div class="max-w-6xl mx-auto px-4">
      <h2 class="text-2xl md:text-3xl font-bold text-center">How it works</h2>
      <ol class="mt-8 grid gap-4 md:grid-cols-3">
        <li class="relative rounded-xl p-5 ring-1 ring-white/30 bg-white/25 backdrop-blur">
          <div class="absolute -top-3 -left-3 h-8 w-8 rounded-full grid place-items-center text-sm font-bold ring-1 ring-white/40 bg-white/70">1</div>
          <h3 class="font-semibold">Connect</h3>
          <p class="mt-1 text-sm text-slate-700/90">Use WhatsApp Cloud API/Interakt. No app installs for users.</p>
        </li>
        <li class="relative rounded-xl p-5 ring-1 ring-white/30 bg-white/25 backdrop-blur">
          <div class="absolute -top-3 -left-3 h-8 w-8 rounded-full grid place-items-center text-sm font-bold ring-1 ring-white/40 bg-white/70">2</div>
          <h3 class="font-semibold">Configure</h3>
          <p class="mt-1 text-sm text-slate-700/90">Pick a module, set windows &amp; rules, mirror copy in local languages.</p>
        </li>
        <li class="relative rounded-xl p-5 ring-1 ring-white/30 bg-white/25 backdrop-blur">
          <div class="absolute -top-3 -left-3 h-8 w-8 rounded-full grid place-items-center text-sm font-bold ring-1 ring-white/40 bg-white/70">3</div>
          <h3 class="font-semibold">Go live</h3>
          <p class="mt-1 text-sm text-slate-700/90">Measure completions, recover misses, escalate edge cases to staff.</p>
        </li>
      </ol>
    </div>
  </section>

  <!-- WHY TAPREBOOK (2×3 rationale grid for variety) -->
  <section class="py-12 border-t border-white/20">
    <div class="max-w-6xl mx-auto px-4">
      <h2 class="text-2xl md:text-3xl font-bold text-center">Why TapRebook</h2>
      <div class="mt-8 grid gap-4 md:grid-cols-3">
        <div class="rounded-2xl p-6 ring-1 ring-white/30 bg-white/25 backdrop-blur">
          <h3 class="font-semibold">WhatsApp-first UX</h3>
          <p class="mt-2 text-slate-700/90">Conversations drive bookings, reminders, and updates—no app installs.</p>
        </div>
        <div class="rounded-2xl p-6 ring-1 ring-white/30 bg-white/25 backdrop-blur">
          <h3 class="font-semibold">Always on</h3>
          <p class="mt-2 text-slate-700/90">Responds instantly 24/7 to reduce wait times and frustration.</p>
        </div>
        <div class="rounded-2xl p-6 ring-1 ring-white/30 bg-white/25 backdrop-blur">
          <h3 class="font-semibold">Measurable outcomes</h3>
          <p class="mt-2 text-slate-700/90">Track completions, drop-offs, and exceptions—escalate when needed.</p>
        </div>
        <div class="rounded-2xl p-6 ring-1 ring-white/30 bg-white/25 backdrop-blur">
          <h3 class="font-semibold">One brand, many modules</h3>
          <p class="mt-2 text-slate-700/90">Reception, Labs, Pharma, DentalLab, Stay, Campus, and Queue.</p>
        </div>
        <div class="rounded-2xl p-6 ring-1 ring-white/30 bg-white/25 backdrop-blur">
          <h3 class="font-semibold">Familiar for staff</h3>
          <p class="mt-2 text-slate-700/90">Shared buttons, replies, and tags reduce retraining and errors.</p>
        </div>
        <div class="rounded-2xl p-6 ring-1 ring-white/30 bg-white/25 backdrop-blur">
          <h3 class="font-semibold">Brand-consistent</h3>
          <p class="mt-2 text-slate-700/90">Templates share tone, style, and languages across locations.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- MODULE STRIP (horizontal scroll; feels different) -->
  <section class="py-10 border-t border-white/20">
    <div class="max-w-6xl mx-auto px-4">
      <div class="flex items-end justify-between gap-4">
        <h2 class="text-2xl md:text-3xl font-bold">What can you standardize?</h2>
        <a class="text-sm underline underline-offset-4 hover:opacity-80" href="/modules/">View all modules</a>
      </div>

      <div class="mt-6 overflow-x-auto [-ms-overflow-style:none] [scrollbar-width:none]"
           style="scroll-snap-type: x mandatory;"
      >
        <div class="flex gap-4 min-w-[720px]">
          {[
            {href:'/modules/reception/', title:'Reception', desc:'Confirmations, reminders, running-late, rescheduling, no-show recovery.'},
            {href:'/modules/labs/', title:'Labs', desc:'Prep instructions, nudges, status (sample/scan/report), results share.'},
            {href:'/modules/pharma/', title:'Pharma', desc:'Refill nudges, chronic-care schedules, escalation to staff, audit.'},
            {href:'/modules/dentallab/', title:'DentalLab', desc:'Pickups, approvals, status, and exception notes between dentist & lab.'},
            {href:'/modules/stay/', title:'Stay', desc:'Pre-arrival info, check-in nudges, upsells, and post-stay reviews.'},
            {href:'/modules/campus/', title:'Campus', desc:'Notices, fees, attendance, events, and two-way updates with audit.'},
            {href:'/modules/queue/', title:'Queue', desc:'Waitlists via WhatsApp, live position, preorders, and table-ready alerts.'},
          ].map((m) => (
            <a href={m.href}
               class="snap-start shrink-0 w-[320px] rounded-2xl p-5 ring-1 ring-white/30 bg-white/20 backdrop-blur hover:opacity-90 transition">
              <h3 class="font-semibold">{m.title}</h3>
              <p class="mt-1 text-sm text-slate-700/90">{m.desc}</p>
              <div class="mt-3 text-xs text-slate-600/80">Learn more →</div>
            </a>
          ))}
        </div>
      </div>
    </div>
  </section>

  <!-- SOCIAL PROOF (unchanged content; different page scaffolding already makes it feel new) -->
  <section class="py-10 border-t border-white/20">
    <div class="max-w-6xl mx-auto px-4">
      <h2 class="text-2xl md:text-3xl font-bold text-center">What customers say</h2>
      <p class="mt-2 text-center text-slate-700/90">Real feedback from pilots and early adopters.</p>

      <div class="mt-8 grid gap-4 md:grid-cols-3">
        {[
          {quote: 'Reminders + self-rescheduling cut our no-shows noticeably within weeks.', name: 'Clinic Admin'},
          {quote: 'Parents acknowledge notices quickly—calls to the office are way down.', name: 'School Coordinator'},
          {quote: 'Queue updates stopped the “how long now?” questions at the host stand.', name: 'Restaurant Manager'},
        ].map((r) => (
          <figure class="rounded-2xl p-5 ring-1 ring-white/30 bg-white/25 backdrop-blur">
            <div class="flex items-center gap-1 text-amber-500" aria-hidden="true">
              {Array.from({ length: 5 }).map(() => <span>★</span>)}
            </div>
            <blockquote class="mt-2 text-sm text-slate-800/90">“{r.quote}”</blockquote>
            <figcaption class="mt-2 text-xs text-slate-600/90">— {r.name}</figcaption>
          </figure>
        ))}
      </div>
    </div>
  </section>

  <!-- CTA -->
  <section class="py-12">
    <div class="max-w-5xl mx-auto px-4 md:flex items-center justify-between">
      <div>
        <h2 class="text-2xl md:text-3xl font-bold">Not sure where to start?</h2>
        <p class="mt-2 text-slate-700/90">
          Tell us your goal; we’ll recommend a starting module in 10 minutes.
        </p>
      </div>
      <div class="mt-6 md:mt-0 flex gap-3">
        <a class="btn btn-primary" href="/modules/">Explore solutions</a>
        <a class="btn btn-secondary" href="/contact/">Book a demo</a>
      </div>
    </div>
  </section>
</Layout>

